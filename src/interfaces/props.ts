export interface NavItemProps {
    title: String
}

export interface CardProps {
    title: String,
    href: String,
    technologies: String,
    desc: String,
    date: String
}

export interface ContactProps {
    prefix: String
}